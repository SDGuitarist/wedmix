# Codex Repo Instructions

## Repo Snapshot
- Project: Early skeleton for an AI wedding-music setlist generator using Spotify and GPT.
- Read first: `README.md`, then `package.json`.
- Main code: no source directory was detected yet
- Tests: none configured
- Config: `package.json`

## Commands
- Install dependencies: `npm install` — installs packages if more code is added later.
- Run tests: `npm test` — currently fails on purpose because the repo still has the default placeholder test script.
- Run app: no runnable app command was detected yet.

## Branch And PR Notes
- Base branch appears to be `main`.
- If PR commands are needed, use `gh pr create --base main`.

## How Codex Should Work Here
- Use Codex for planning what should exist next, explaining the current skeleton in plain English, and reviewing risk once real code exists.
- Do not invent a full architecture without first confirming the repo actually needs it.
- When asked to plan, keep it to one concrete next slice.
- When asked to review, call out missing foundations and missing tests clearly.

## Repo Guardrails
- This repo is incomplete. Prefer explicit unknowns over confident guesses.
- Do not pretend a production app exists here yet.
